<?php$controller = 'controller';$model = 'model';$view = 'view';$system = 'system';define('DS', DIRECTORY_SEPARATOR,true);define('droot', realpath(dirname(__FILE__)).ds,true);if ( ! is_dir($controller) AND is_dir(droot.$controller))$controller = droot.$controller;if ( ! is_dir($model) AND is_dir(droot.$model))$model = droot.$model;if ( ! is_dir($system) AND is_dir(droot.$system))$system = droot.$system;define('dcontroller',realpath($controller).ds);define('dmodel', realpath($model).ds);define('dview', realpath($view).ds);define('dsystem', realpath($system).ds);unset($controller, $model,$view,$system);define('URL', strtolower($_SERVER['HTTP_HOST']),true);define('ext', ".php",true);require_once (dsystem."init".ext);